<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <item android:title="Menú">
        <menu>
            <item
                android:id="@+id/nav_matricula"
                android:icon="@android:drawable/ic_menu_today"
                android:title="@string/item_matricula" />
            <item
                android:id="@+id/nav_historial"
                android:icon="@android:drawable/ic_menu_month"
                android:title="@string/item_historial" />
            <item
                android:id="@+id/nav_cursos"
                android:icon="@android:drawable/ic_menu_sort_by_size"
                android:title="@string/item_cursos" />
            <item
                android:id="@+id/nav_estudiantes"
                android:icon="@android:drawable/ic_menu_sort_by_size"
                android:title="@string/item_estudiantes" />
        </menu>
    </item>

    <item android:title="Opciones de Usuario">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Cerrar sección" />
        </menu>
    </item>

</menu>
